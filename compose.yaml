version: "3.8"

services:
  ski-aggregator:
    build: .
    environment:
      - PORT=5001
      - DB_PATH=/data/resorts.db
    restart: unless-stopped
    depends_on:
      - cache
      
  cache:
    image: redis:7-alpine
    restart: unless-stopped
